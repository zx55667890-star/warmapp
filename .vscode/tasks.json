{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run App",
            "type": "shell",
            "command": "./gradlew :app:assembleDebug; adb install -r app/build/outputs/apk/debug/app-debug.apk; adb shell am start -n com.example.myapplication/.MainActivity",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}